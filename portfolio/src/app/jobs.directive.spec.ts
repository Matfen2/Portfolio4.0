import { JobsDirective } from './jobs.directive';

describe('JobsDirective', () => {
  it('should create an instance', () => {
    const directive = new JobsDirective();
    expect(directive).toBeTruthy();
  });
});
